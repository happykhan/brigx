{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Narrow BRIGX desktop capability for the bundled main window",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": [
    "desktop",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
